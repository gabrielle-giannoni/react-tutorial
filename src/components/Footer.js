import Box from "@mui/material/Box";
import { height } from "@mui/system";
const Footer = () => {
    return (
    <Box sx={{
        background: '#555',
        height: "60px"
    }}>
        <h2>Footer</h2>
    </Box>
    )
}

export default Footer